import products from '../data/productsData'

const initialState = {
  products,
}

export const shopListReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
